# Scripts

Operational entrypoints are grouped by concern.

- `api/`
  Service startup scripts.
- `pipelines/`
  Offline and online pipeline runners for ingestion, indexing, retrieval, reranking, context, generation, and post-processing.
- `evaluation/`
  Benchmark dataset generation and experiment runners.
- `shared/`
  Script bootstrap helpers for path setup.
